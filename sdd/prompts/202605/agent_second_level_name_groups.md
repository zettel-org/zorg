---
plan: sdd/tales/202605/agent_second_level_name_groups.md
---
 Can you help me add another (uniquely styled) group level on the "Agents" tab of the `sase ace` TUI for all agents whose name starts with the same `<foo>.<bar>.`? This new group would then, in
our example, be nested under the group containing all agents whose name starts with `<foo>.` (which is already implemented). I want you to lead the design on this one. Just make sure it looks beautiful! Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
