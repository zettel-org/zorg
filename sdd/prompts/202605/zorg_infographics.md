---
plan: sdd/epics/202605/zorg_infographics.md
---
  Can you help me use GPT image to generate 7 infographics, save those images to the appropriate zorg
project repo, and then reference it in one of that repo's documentation markdown files (i.e. the README.md file or one
of the docs/ files)? You should decide which images to generate on what topic based on which zorg concepts you feel
would benefit the most from a visual illustration. This is a large piece of work that should be split into phases. I'll let you decide how many phases to create, but
keep in mind that each phase will be completed by a distinct agent instance (i.e. a distinct `claude` / `gemini` /
`codex` command). Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.

